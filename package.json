{
  "name": "maily",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm --workspace client run dev",
    "build": "npm --workspace client run build -- --outDir ../dist --emptyOutDir",
    "preview": "npm --workspace client run preview -- --outDir ../dist",
    "start:server": "npm --workspace server run start",
    "dev:server": "npm --workspace server run dev"
  }
}
