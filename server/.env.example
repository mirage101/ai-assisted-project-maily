# Server environment variables (copy to .env for local dev)
NODE_ENV=development
PORT=5000

# MongoDB
# Local example:
# MONGO_URI=mongodb://localhost:27017/maily
# Atlas example:
# MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/maily

# JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE=30d

# CORS
# Comma-separated list of allowed frontend origins.
# Example: CLIENT_URLS=http://localhost:5173,https://your-app.vercel.app
CLIENT_URLS=http://localhost:5173

# If true, also allow any https://*.vercel.app origin (useful for preview deployments)
ALLOW_VERCEL_PREVIEWS=false
